# HOSTING-GUIDE.md — از «کد آماده» به «فروشگاه زنده»

> این سند به یک سؤال ساده جواب می‌دهد: **چطور این پروژه واقعاً پول دربیاورد؟**

---

## ۱. اول یک حقیقت صریح

**آپلود روی گیت‌هاب، فروشگاه را روی اینترنت نمی‌آورد.** گیت‌هاب یک «کتابخانه» است، نه «مغازه». با آپلود کردن:

* یک **نسخه پشتیبان** مطمئن از کد ساخته می‌شود (کد الان فقط در فضای کاری من است و آن فضا موقتی است)؛
* **تاریخچه** تغییرات ثبت می‌شود (چه کسی، کِی، چه چیزی را عوض کرد)؛
* تست‌های خودکار (CI) بعد از هر تغییر اجرا می‌شوند؛
* و مهم‌تر از همه: **نصب روی سرور به یک دستور تبدیل می‌شود** — به‌جای آپلود دستی فایل‌ها با FTP.

اما **هیچ مشتری با آپلود گیت‌هاب نمی‌تواند خرید کند.** برای فروش، فروشگاه باید روی یک **سرور** نصب شود.
من از این فضای کاری نمی‌توانم به سرور تو وصل شوم (دسترسی شبکه‌ای بیرونی ندارم)، بنابراین آن یک قدم را تو (یا هر کسی که این راهنما را می‌خواند) در ۱۵ دقیقه انجام می‌دهد. **بقیه‌اش را من آماده کرده‌ام.**

---

## ۲. یک فروشگاه اینترنتی به چه چیزی نیاز دارد؟

| # | نیاز | چیست | چه کسی انجام می‌دهد |
|:-:|---|---|---|
| ۱ | **سرور** (VPS) | یک کامپیوتر اجاره‌ای که همیشه روشن و به اینترنت وصل است | خرید توسط تو (۱۰ دقیقه) |
| ۲ | **دامنه** | آدرس فروشگاه، مثل `mantomoda.ir` | خرید توسط تو (۱۰ دقیقه) |
| ۳ | **HTTPS** | قفل سبز کنار آدرس؛ **درگاه پرداخت بدون آن کار نمی‌کند** | خودکار (اسکریپت من نصب می‌کند، رایگان) |
| ۴ | **کلیدهای درگاه پرداخت و پیامک** | `ZARINPAL_MERCHANT_ID` و `KAVENEGAR_API_KEY` | گرفتن از زرین‌پال/کاوه‌نگار (تصمیم تجاری تو) |

---

## ۳. هزینه‌ها (تقریبی — قیمت‌ها عوض می‌شوند)

| مورد | هزینه تقریبی | یادداشت |
|---|---|---|
| سرور ایرانی (۱ هسته، ۲ گیگ رم، ۴۰ گیگ دیسک) | ماهی حدود ۱ تا ۳ میلیون تومان | برای شروع و صدها بازدید روزانه کافی است |
| دامنه `.ir` یا `.com` | سالی حدود ۲۰۰ هزار تا ۱ میلیون تومان | از ثبت‌کننده‌های ایرانی |
| گواهی HTTPS | **رایگان** | Let's Encrypt، خودکار تمدید می‌شود |
| زرین‌پال | کارمزد روی هر تراکنش | بدون هزینه ثابت ماهانه |
| کاوه‌نگار | به‌ازای هر پیامک | پیامک تأیید سفارش و هشدار موجودی |

**جمع ماهانه واقع‌بینانه: حدود ۱ تا ۳ میلیون تومان** برای یک فروشگاه فعال. (این اعداد را قبل از خرید از خود سرویس‌دهنده بگیر.)

---

## ۴. توصیه من (انتخاب فنی، با دلیل)

**سرور ایرانی + Docker.** دلیل‌ها:

1. **مشتری ایرانی**: سرور داخلی برای مشتری‌های داخل ایران به‌مراتب سریع‌تر است (بدون مشکل تحریم و فیلترینگ).
2. **درگاه پرداخت زرین‌پال** برای بازگشت به سایت باید سریع و پایدار باشد؛ سرور داخلی این را تضمین می‌کند.
3. **Docker** یعنی نصب و به‌روزرسانی یک دستور است و روی هر سرور Ubuntu یکسان کار می‌کند — مهم‌ترین نکته برای کسی که برنامه‌نویس نیست.

سرویس‌دهنده‌های رایج ایرانی که این نوع سرور را می‌فروشند: پارس‌پک، ابر آروان، میزبان‌فا، سرور ابری‌های دیگر. **معیار انتخاب، نه نام برند:**
لوکیشن ایران • Ubuntu 24.04 • حداقل ۱ هسته CPU و ۲ گیگ رم و ۴۰ گیگ SSD • دسترسی root با SSH.

---

## ۵. گام‌به‌گام (کوتاه‌ترین مسیر)

### گام ۱ — سرور بگیر (تو)
موقع خرید، این مشخصات را بده:
> Ubuntu 24.04 — ۱ هسته CPU — ۲ گیگ رم — ۴۰ گیگ SSD — لوکیشن ایران

بعد از خرید، دو چیز به تو می‌دهند که لازم است: **آدرس IP سرور** و **رمز root**.

### گام ۲ — دامنه بگیر و به سرور وصل کن (تو)
۱. دامنه را از یک ثبت‌کننده ایرانی بخر (`mantomoda.ir` یا هر نامی که خواستی).
۲. در پنل دامنه، بخش **DNS**، این دو رکورد را بساز:

| نوع | نام | مقدار |
|---|---|---|
| A | `@` | آدرس IP سرور |
| A | `www` | آدرس IP سرور |

⏳ انتشار DNS بین ۱۵ دقیقه تا چند ساعت طول می‌کشد. صبر کن تا با `ping mantomoda.ir` جواب IP سرورت را بدهد.

### گام ۳ — یک دستور روی سرور (کپی‌پیست)
وارد ترمینال سرور شو (هر سرویس‌دهنده یک «کنسول» یا «ترمینال» در پنل دارد؛ اگر نداشت، با نرم‌افزار SSH مثل PuTTY وصل شو و `root@آی‌پی‌سرور` را بزن).

```bash
# ۱) دریافت کد پروژه (اگر روی گیت‌هاب آپلود کرده‌ای)
apt-get update && apt-get install -y git
git clone https://github.com/ab1234554321a-tech/mantomoda.2.git /srv/mantomoda-src
cd /srv/mantomoda-src

# ۲) نصب کامل با یک دستور (دامنه خودت را بگذار)
DOMAIN=mantomoda.ir bash scripts/server-install.sh
```

> ⚠️ **در پایان نصب، دو خط مهم چاپ می‌شود: ایمیل و رمز ورود پنل مدیریت.** همان لحظه یادداشتشان کن؛
> رمز فقط همان یک بار روی صفحه می‌آید (بعداً هم داخل فایل `$APP_DIR/.env` هست). با آن از دکمه
> «ورود مدیر» در سایت، وارد پنل می‌شوی. اگر رمز را عوض کردی، `docker compose restart manto` را بزن.

### گام ۴ — چه چیزی در فروشگاه واقعی هست و چه چیزی نیست
فروشگاه تو **خالی** بالا می‌آید: نه کالای نمونه، نه سفارش جعلی، نه مشتری ساختگی. این عمدی است —
داده‌های آزمایشی روی فروشگاه واقعی، هم گیج‌کننده‌اند هم از نظر حریم خصوصی درست نیستند.
- کالاها را از پنل مدیریت → محصولات اضافه کن (عکس، رنگ، سایز، قیمت خرده و عمده).
- «شبیه‌ساز نقش» و حساب‌های آزمایشی، روی سرور واقعی **کاملاً خاموش‌اند** (ADR-024): ابزار دمو است و
  اجازه نمی‌دهد کسی بدون رمز، توکن مدیر بگیرد. اگر برای نمایش موقت روی سرور به آن نیاز داشتی،
  `ALLOW_DEMO_MODE=true` را در `.env` بگذار — ولی برای فروشگاه واقعی هرگز.

همین. این دستور: Docker را نصب می‌کند، کاربر سرویس می‌سازد، داده و عکس‌ها را در مسیر پایدار می‌گذارد، رمز امنیتی تصادفی می‌سازد، فروشگاه را بالا می‌آورد، Nginx و گواهی HTTPS رایگان را نصب می‌کند، پشتیبان‌گیری روزانه را زمان‌بندی می‌کند و در پایان همه‌چیز را بررسی می‌کند.

> **اگر روی گیت‌هاب آپلود نکردی:** فایل زیپ را با SFTP در سرور بگذار، بعد:
> `apt-get install -y unzip && unzip mantomoda-updated.zip && cd mantomoda && DOMAIN=دامنه bash scripts/server-install.sh`
> (این همان «درد» آپلود گیت‌هاب است — نسخه گیت‌هاب یک دستور کمتر دارد.)

### گام ۴ — کلیدها و تنظیمات تجاری (تو، ۵ دقیقه)
یک فایل روی سرور را با این دستور باز کن:

```bash
nano /srv/mantomoda/.env
```

این سه خط را با مقادیر واقعی پر کن (بقیه را دست نزن):

```
ZARINPAL_MERCHANT_ID=کد-دریافتی-از-زرین‌پال
KAVENEGAR_API_KEY=کلید-دریافتی-از-کاوه‌نگار
KAVENEGAR_SENDER=شماره-فرستنده-تأییدشده
```

ذخیره: `Ctrl+O` بعد `Enter` بعد `Ctrl+X`. سپس:

```bash
cd /srv/mantomoda && docker compose restart manto
```

و در **پنل مدیریت → تنظیمات فروشگاه**: تعرفه ارسال واقعی و **موبایل خودت** را وارد کن (هشدار کم‌موجودی برایت پیامک می‌شود).

---

## ۶. چک‌لیست ۵ دقیقه‌ای بعد از انتشار (خودت انجام بده)

| # | کار | نتیجه درست |
|:-:|---|---|
| ۱ | آدرس `https://دامنه` را باز کن | صفحه اصلی با قفل سبز باز می‌شود |
| ۲ | `https://دامنه/robots.txt` و `/sitemap.xml` | هر دو باز می‌شوند |
| ۳ | ورود با شماره موبایل خودت | پیامک کد تأیید می‌آید و وارد می‌شوی |
| ۴ | یک محصول ارزان بساز (مثلاً ۱۰ هزار تومان) و یک سفارش بده | به درگاه زرین‌پال می‌رود |
| ۵ | پرداخت را کامل کن | برمی‌گردد به صفحه نتیجه و وضعیت پرداخت «پرداخت شده» است |
| ۶ | از پنل، وضعیت سفارش را «تأیید» کن | پیامک تغییر وضعیت به موبایلت می‌آید (و **باید** بیاید) |
| ۷ | پنل → سفارش‌ها → **لینک فاکتور** | فاکتور قابل چاپ باز می‌شود |
| ۸ | پنل → سفارش‌ها → **خروجی اکسل** | فایل CSV دانلود می‌شود |
| ۹ | پنل → تنظیمات → موبایل خودت را ثبت کن، یک محصول را موجودی‌اش را صفر کن | پیامک «هشدار موجودی» می‌آید |
| ۱۰ | روی سرور: `cd /srv/mantomoda && bash scripts/backup.sh` | یک فایل پشتیبان در `backups/` ساخته می‌شود |

اگر همه ۱۰ مورد ✅ شد، فروشگاه آماده فروش واقعی است.
**محصول آزمایشی را یادت نرود بعداً آرشیو کنی** (پنل → محصولات → برداشتن از فروشگاه).

---

## ۷. کارهای روزمره (فقط همین چند دستور)

```bash
cd /srv/mantomoda

docker compose ps                      # وضعیت فروشگاه
docker compose logs -f --tail=100 manto  # دیدن پیام‌ها و خطاها
docker compose restart manto           # راه‌اندازی مجدد
bash scripts/preflight.sh              # بررسی سلامت و تنظیمات
bash scripts/backup.sh                 # پشتیبان فوری
bash scripts/backup.sh --list          # فهرست پشتیبان‌ها
```

**به‌روزرسانی کد در آینده:**

```bash
cd /srv/mantomoda
docker compose down
# فایل‌های کد جدید را جای‌گزین کن (یا git pull)
docker compose up -d --build
```

⚠️ **هرگز پوشه `data/` را پاک نکن** — سفارش‌های واقعی و عکس‌های محصولات آنجاست.

**بازیابی از پشتیبان:**

```bash
bash scripts/backup.sh --restore /srv/mantomoda/backups/manto-backup-YYYYMMDD-HHMMSS.tar.gz
docker compose restart manto
```

---

## ۸. رفع اشکال‌های رایج

| نشانه | دلیل معمول | راه‌حل |
|---|---|---|
| سایت باز نمی‌شود | DNS هنوز منتشر نشده یا Nginx نصب نشده | `ping دامنه` سپس `systemctl status nginx` |
| `EADDRINUSE` در لاگ | یک نسخه دیگر روی همان پورت در حال اجراست | `docker compose down` بعد `up -d` |
| سرور بالا نمی‌آید و پیام `mock` می‌دهد | `PAYMENT_PROVIDER=mock` در production | در `.env` مقدار `zarinpal` بگذار یا برای تست موقت `ALLOW_MOCK_PROVIDERS=true` |
| پرداخت انجام می‌شود ولی سفارش «پرداخت‌نشده» می‌ماند | آدرس بازگشت درگاه اشتباه است | `bash scripts/preflight.sh` را ببین؛ `PAYMENT_CALLBACK_BASE_URL` باید `https://دامنه` باشد |
| پیامک نمی‌آید | کلید یا شماره فرستنده اشتباه است | `docker compose logs -f manto` و خطای ارسال پیامک را بخوان |
| عکس آپلود نمی‌شود | حجم بالای ۵ مگابایت یا فرمت غیرمجاز | فقط JPEG/PNG/WebP، حداکثر ۵ مگابایت |
| **همه سفارش‌ها پاک شده‌اند** | `data/` جابه‌جا یا پاک شده، یا `PERSIST_DATA=false` | از پشتیبان بازیابی کن؛ `bash scripts/preflight.sh` این را از قبل هشدار می‌دهد |
| سایت کند است | سرور ضعیف یا ترافیک بالا | ارتقای رم/CPU یا افزودن کش (لایه بعدی پروژه) |

---

## ۹. چه چیزهایی الان روی سرور خودکار است؟

* **بازگشت خودکار** پس از ری‌استارت سرور (`restart: unless-stopped` و systemd).
* **ذخیره‌سازی امن داده**: نوشتن اتمیک روی دیسک + flush هنگام خاموشی (ADR-010).
* **پشتیبان روزانه ساعت ۳:۳۰ بامداد** با نگه‌داری ۳۰ نسخه و بررسی سلامت آرشیو.
* **گواهی HTTPS با تمدید خودکار**.
* **بازرسی وضعیت** هر ۳۰ ثانیه (Docker healthcheck).
* **گیت تست**: ۸ مجموعه تست و ۱۷ بررسی دسترس‌پذیری قبل از هر تغییر روی `main` (CI).

---

## ۱۰. چه چیزی هنوز کار توست؟

۱. خرید سرور و دامنه (بدون این‌ها فروشگاهی وجود ندارد).
۲. گرفتن کلید زرین‌پال و کاوه‌نگار (تصمیم تجاری و احراز هویت).
۳. اجرای همان یک دستور روی سرور.
۴. انجام چک‌لیست بخش ۶ (۱۰ دقیقه).

هر جای این مسیر گیر کردی، لاگ یا پیام خطا را برایم بفرست — دقیقاً می‌گویم کدام خط را عوض کن.
