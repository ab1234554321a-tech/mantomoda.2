# PHASE-10-PLAN.md — فاز ۱۰: خودگردانی فروشگاه (پنل مدیریت کامل، قیمت‌گذاری، تخفیف، فاکتور)

> **این فهرست را من انتخاب کردم و اجرا می‌کنم — بدون پرسیدن.**
> مبنای انتخاب: شکاف‌هایی که با خواندن کد پیدا کردم، نه حدس.
> شروع: ۲۰۲۶-۰۹-۲۰ (بلافاصله بعد از پایان فاز ۹.۵)

---

## چرا این فاز؟

فاز ۹.۵ فروشگاه را **امن و قابل‌اعتماد** کرد (انبار، ماندگاری، وضعیت سفارش، سئو، پشتیبان).
اما هنوز یک مشکل بزرگ باقی است: **صاحب فروشگاه نمی‌تواند بدون برنامه‌نویس فروشگاه را بچرخاند.**

شاهدهای عینی از کد (همین امروز بررسی شد):

| شکاف | شاهد در کد |
|---|---|
| پنل ادمین محصول جدید نمی‌سازد | API `POST /api/admin/products` هست، ولی در `src/client/public/app.js` هیچ فرمی برای آن وجود ندارد |
| داده محصول اعتبارسنجی نمی‌شود | در `admin.routes.js` فقط وجود `title` و `retailPrice` بررسی می‌شود؛ قیمت منفی، موجودی متنی و اسلاگ تکراری قابل ثبت است |
| تنوع کالا (رنگ/سایز) قابل ویرایش نیست | هیچ واسطی برای `variants[]` وجود ندارد |
| قانون ارسال در دو فایل تکرار شده | `cart.routes.js:115` و `order.routes.js:67` هر دو `subtotal > 2000000 … : 45000` را جدا نوشته‌اند → خطر واگرایی |
| کد تخفیف وجود ندارد | فیلد `discountAmount` در سفارش هست، ولی هیچ‌جا مقدار نمی‌گیرد |
| فاکتور وجود ندارد | هیچ خروجی قابل‌چاپ یا قابل‌ارسال برای سفارش نیست |
| حسابداری دستی است | `GET /api/admin/orders` بدون جست‌وجو، فیلتر تاریخ و خروجی اکسل |
| معلوم نیست چه کسی چه چیزی را عوض کرد | هیچ لاگ اقدامات ادمینی وجود ندارد |
| آمار پنل خام است | `totalRevenue` روی همه سفارش‌ها (حتی لغوشده‌ها) جمع می‌زند |

---

## وضعیت اجرا

| # | کار | چرا (اثر کسب‌وکار) | وضعیت |
|:-:|---|---|:-:|
| ۱ | تنظیمات فروشگاه + **سیاست ارسال یکپارچه** | حذف قانون تکراری؛ صاحب فروشگاه خودش هزینه ارسال و آستانه ارسال رایگان را عوض می‌کند | ✅ |
| ۲ | **موتور کد تخفیف** (درصدی/مبلغی، سقف، تاریخ انقضا، محدودیت تعداد) | ابزار اصلی کمپین فروش و تخلیه انبار | ✅ |
| ۳ | اعتبارسنجی سخت‌گیرانه محصول + تولید اسلاگ و SKU یکتا | جلوگیری از ثبت داده نامعتبر که انبار و مالی را خراب می‌کند | ✅ |
| ۴ | پنل: **ساخت/ویرایش/آرشیو محصول + تنوع‌ها** | بدون آن هر محصول جدید = یک تماس با برنامه‌نویس | ✅ |
| ۵ | ویرایش سریع موجودی + **هشدار کم‌موجودی پیامکی** | جلوگیری از فروش کالای ناموجود، بدون چک کردن دستی | ✅ |
| ۶ | سفارش‌ها: جست‌وجو، فیلتر وضعیت/تاریخ، صفحه‌بندی + **خروجی اکسل** | حسابداری و پاسخ سریع به «سفارش من کجاست؟» | ✅ |
| ۷ | **فاکتور قابل چاپ** (لینک امضاشده) | مشتری و حسابدار هر دو فاکتور رسمی می‌خواهند | ✅ |
| ۸ | داشبورد واقعی: درآمد امروز/ماه، پرفروش‌ها، موجودی بحرانی | تصمیم‌گیری روزانه صاحب فروشگاه | ✅ |
| ۹ | **لاگ اقدامات ادمین** (کی، کِی، چه چیزی را عوض کرد) | پاسخگویی و بازگردانی خطای انسانی | ✅ |
| ۱۰ | تست‌های سطح ۸ (فروش و بازرگانی) روی HTTP واقعی | هر قابلیت جدید پشت تست | ✅ |
| ۱۱ | مستندات (ADR-017..۰۲۰) + کامیت + بسته آپلود | قانون ۱۰ پروژه | ✅ |

### شاهدهای اجرا (۲۰۲۶-۰۹-۲۰)
- **تنظیمات:** تعرفه تهران ۳۵٬۰۰۰ و اردبیل ۴۵٬۰۰۰؛ سبد خرید با تغییر استان بلافاصله عدد درست را نشان داد.
- **کد تخفیف:** `EID1405` (۲۵٪، سقف ۸۰۰٬۰۰۰) → تخفیف ۴۶۲٬۵۰۰ روی سبد ۱٬۸۵۰٬۰۰۰؛ کد تکراری ۴۰۹، درصد ۹۹ رد شد.
- **محصول:** «تست منفی» و «بدون تنوع» و «قیمت عمده بالاتر از تک» هر سه رد شدند؛ محصول درست کد کالای لاتین `MM-FORMAL-MANTO-SUMMER-LINEN` و اسلاگ فارسی گرفت و در سایتورمال آمد.
- **انبار:** با موجودی ۱ به صفر، پیامک «هشدار موجودی … تمام شد» برای صاحب فروشگاه ارسال شد؛ با شماره نامعتبر، ویرایش انبار همچنان موفق ماند.
- **سفارش‌ها:** جست‌وجو با نام «ندa» و شماره سفارش، خروجی CSV با BOM (ef bb bf) و ستون‌های فارسی.
- **فاکتور:** لینک امضاشده باز شد (۲۰۰)، لینک دستکاری‌شده ۴۰۳، بدون لینک ۴۰۳، مبلغ با اعداد فارسی ۱٬۴۲۲٬۵۰۰ و `noindex`.
- **داشبورد:** فروش تأییدشده، در انتظار پرداخت، پرفروش‌ها و موجودی بحرانی؛ لغوشده‌ها از درآمد حذف شده‌اند.
- **لاگ اقدامات:** ۱۴+ رکورد با ایمیل ادمین و زمان؛ کاربر عادی ۴۰۳.
- **تست‌ها:** دو باگ واقعی توسط مجموعه هشتم گرفته شد (حذف `couponCode` توسط اسکیمای Zod و ذخیره‌نشدن کد تخفیف روی سفارش).

## اصول اجرا (مثل فاز قبل)
- به همان الگوی موجود کد می‌نویسم (ماژولار، کامنت انگلیسی، پیام کاربر فارسی).
- هر قانون کسب‌وکار **یک جا** تعریف می‌شود، نه در چند فایل.
- محدودیت‌ها سمت سرور اعمال می‌شوند؛ UI فقط بازتاب آن‌هاست.
- بعد از هر آیتم: `npm test` + `npm run a11y`.
- تصمیم‌های معماری در `DECISIONS.md` ثبت می‌شوند.

## تصمیم‌هایی که خودم گرفتم (و دلیلم)
1. **حذف محصول = آرشیو، نه پاک کردن فیزیکی.** چون سفارش‌های گذشته به آن محصول ارجاع می‌دهند؛ پاک کردن یعنی خرابی تاریخچه مالی.
2. **اسلاگ پس از ساخت تغییر نمی‌کند** (حتی اگر عنوان عوض شود) — چون URL فروخته‌شده در اینستاگرام و ایندکس گوگل باید معتبر بماند.
3. **تخفیف همیشه سمت سرور دوباره محاسبه می‌شود** — همان اصل ADR-008 (مبلغ کلاینت هرگز قابل اعتماد نیست).
4. **فاکتور با لینک امضاشده (HMAC) و انقضادار** — نه با ورود اجباری (مشتری ممکن است فاکتور را برای حسابدار بفرستد)، نه با لینک عمومی (آدرس مشتری لو می‌رود).
5. **هزینه ارسال بر اساس استان** با مقدار پیش‌فرض سراسری؛ ارسال رایگان برای عمده‌فروش تأییدشده و سفارش بالای آستانه.
