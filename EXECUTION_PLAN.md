# EXECUTION_PLAN.md — فهرست کارهای در دست اجرا

> **این فهرست را من اجرا می‌کنم، بدون پرسیدن.** هر مورد پس از اتمام، ✅ می‌شود و مستندات پروژه هم‌زمان به‌روز می‌شود.
> مبنا: یافته‌های `IMPROVEMENT_PLAN.md` (بررسی مستقیم کد، با شاهد).
> شروع: ۲۰۲۶-۰۹-۲۰

---

## وضعیت کلی

| # | کار | چرا (اثر) | وضعیت |
|:-:|---|---|:-:|
| ۱ | لایه ماندگاری داده (فایل + نوشتن اتمیک) | با هر ری‌استارت همه سفارش‌ها/کاربران پاک می‌شوند | ✅ |
| ۲ | یکپارچگی انبار (ضد فروش ناموجود) | تنها باگی که مستقیماً اعتبار برند را می‌سوزاند | ✅ |
| ۳ | ماشین وضعیت سفارش + تاریخچه تغییرات | پرش‌های نامعتبر وضعیت، انبار و مالی را خراب می‌کند | ✅ |
| ۴ | اعلان پیامکی وضعیت سفارش | بیشترین کاهش تماس پشتیبانی (آداپتر آماده است) | ✅ |
| ۵ | مدیریت خطای سطح process + flush در خاموشی | جلوگیری از مرگ خاموش سرور و از دست رفتن داده ذخیره‌نشده | ✅ |
| ۶ | تست‌های HTTP واقعی (سوپرتست) + جدا کردن app از listen | تست‌های فعلی هیچ مسیر HTTP را واقعاً اجرا نمی‌کنند | ✅ |
| ۷ | صفحه‌بندی کاتالوگ | با ۵۰۰ محصول، هر بازدید = چند مگابایت | ✅ |
| ۸ | آپلود عکس محصول (+ تبدیل WebP و بندانگشتی) | بدون آن هیچ محصول واقعی قابل ثبت نیست | ✅ |
| ۹ | پیش‌رندر صفحه محصول + متا تگ + سایت‌مپ و robots | بدون آن گوگل هیچ محصولی را ایندکس نمی‌کند | ✅ |
| ۱۰ | دسترس‌پذیری: alt، aria، label، فوکوس، کنتراست | صفحه‌خوان‌ها نمی‌توانند خرید کنند | ✅ |
| ۱۱ | پشتیبان‌گیری خودکار + دستور بازیابی | داده = کل کسب‌وکار | ✅ |
| ۱۲ | به‌روزرسانی کامل مستندات + کامیت + بسته آپلود | قانون ۱۰ پروژه: وضعیت باید بازتاب واقعیت باشد | ✅ |

---

## جزئیات هر کار

### ۱. لایه ماندگاری داده
- `src/server/db/persistence.js`: بارگذاری در بوت، ذخیره دیبانس‌شده، **نوشتن اتمیک** (tmp + rename)، flush قطعی در SIGTERM.
- غیرفعال در `NODE_ENV=test` (تا تست‌ها آلوده نشوند)، فعال در dev و production.
- `RESET_DATA=true` برای بازگرداندن به داده اولیه. `data/` در `.gitignore`.
- **معیار پذیرش**: سفارش ثبت شود → سرور ری‌استارت شود → سفارش سر جایش باشد.

### ۲. یکپارچگی انبار
- `checkStock` / `reserveStock` / `releaseStock` در همان لایه store.
- بررسی موجودی در سبد خرید (نمایش) و **در تسویه‌حساب (قفل)**؛ سفارش بیش از موجودی → رد با پیام دقیق کالای ناموجود.
- کاهش اتمیک هنگام ثبت سفارش (بدون `await` در میانه) → نبود Race Condition.
- **معیار پذیرش**: تلاش برای خرید ۱۰۰ عدد از موجودی ۳ → رد؛ خرید ۲ عدد → موجودی ۱ شود.

### ۳. ماشین وضعیت سفارش
- نقشه گذارهای مجاز: `PENDING→CONFIRMED|CANCELLED`, `CONFIRMED→PROCESSING|CANCELLED`, `PROCESSING→SHIPPED|CANCELLED`, `SHIPPED→DELIVERED`, `DELIVERED→∅`, `CANCELLED→∅`.
- گذار نامعتبر → `409` با فهرست گذارهای مجاز.
- `statusHistory[]` روی هر سفارش: چه کسی، چه زمانی، از چه وضعیتی به چه وضعیتی.
- لغو سفارش → **بازگشت خودکار موجودی به انبار**.
- **معیار پذیرش**: `DELIVERED→PENDING` رد شود؛ لغو، موجودی را برگرداند.

### ۴. اعلان پیامکی
- `src/server/services/notification.service.js` + متد `sendMessage` در هر دو آداپتر پیامک.
- پیام فارسی برای هر وضعیت (تأیید، آماده‌سازی، ارسال، تحویل، لغو) + پیام «سفارش ثبت شد».
- اعلان **هرگز درخواست را نمی‌شکند**: خطا لاگ و روی سفارش ثبت می‌شود (`notifications[]`).
- **معیار پذیرش**: با آداپتر mock، اعلان در outbox ثبت شود؛ شکست ارسال، تغییر وضعیت را متوقف نکند.

### ۵. پایداری سطح process
- `unhandledRejection` → لاگ (سرور زنده بماند)، `uncaughtException` → لاگ + خاموشی امن.
- خاموشی امن: flush داده‌های ذخیره‌نشده، بستن سرور، کد خروج درست.

### ۶. تست‌های HTTP واقعی
- جدا کردن `src/server/app.js` (ساخت اپ) از `src/server/index.js` (listen + سیگنال‌ها).
- نصب `supertest` به‌عنوان devDependency (چیزی که README از قبل ادعا می‌کرد).
- مجموعه هفتم: `tests/api.test.js` — تست واقعی HTTP روی همه مسیرهای جدید و حیاتی.

### ۷. صفحه‌بندی کاتالوگ
- `GET /api/products?page=&limit=` با `meta: {page, limit, total, totalPages, hasMore}`؛ پیش‌فرض ۱۲، سقف ۶۰.
- کلاینت: دکمه «نمایش بیشتر» + نمایش تعداد نتایج.

### ۸. آپلود عکس محصول
- `multer` + اعتبارسنجی نوع و حجم (≤ ۵MB)، فقط ادمین، ذخیره در `data/uploads`، سرو استاتیک با کش.
- تبدیل به **WebP** و ساخت بندانگشتی در صورت امکان فنی.
- رابط در پنل ادمین: انتخاب فایل برای هر محصول.

### ۹. SEO
- `robots.txt` و `sitemap.xml` (پویا از روی محصولات فعال، با `lastmod`).
- پیش‌رندر `GET /product/:slug`: HTML با `title`، `description`، canonical، OG/Twitter و **JSON-LD Product** (قیمت، موجودی، برند) + بلوک `noscript`.
- متا پایه در `index.html`: description، OG، Twitter، canonical، theme-color، JSON-LD سازمان/سایت.
- لینک‌دهی درون‌سایتی: کارت محصول به URL اختصاصی اشاره کند (قابل اشتراک در اینستاگرام/واتساپ).

### ۱۰. دسترس‌پذیری
- `alt` برای همه تصاویر (کارت، گالری، سبد، سفارش‌ها)، `aria-label` برای دکمه‌های فقط-آیکون.
- `<label>`/`aria-label` برای همه ورودی‌های فرم‌های تسویه، عمده‌فروشی و ادمین.
- `role="dialog"` + `aria-modal` برای مودال‌ها، `aria-live` برای توست‌ها.
- فوکوس قابل مشاهده (`:focus-visible`) در `styles.css`.

### ۱۱. پشتیبان‌گیری
- `scripts/backup.sh`: آرشیو زمان‌دار از `data/`، نگهداری N نسخه آخر، دستور بازیابی مستندشده.
- راهنمای cron روزانه در README.

### ۱۲. مستندات و تحویل
- به‌روزرسانی: `ARCHITECTURE.md`، `CHANGELOG.md`، `TASKS.md`، `BACKLOG.md`، `TECH_DEBT.md`، `PROJECT_STATE.md`، `project-state.json`، `PROJECT_HANDOFF.md`، `README.md`، `IMPROVEMENT_PLAN.md`، `.env.example`.
- ADRهای جدید: ماندگاری، اعلان، پیش‌رندر SEO، آپلود تصویر.
- کامیت‌های معنادار + ساخت دوباره `mantomoda-updated.zip`.

---

## قواعدی که در طول اجرا رعایت می‌کنم

1. **هر گام تمام شود → `npm test` سبز → برو گام بعد.** هیچ گامی نیمه‌کاره کامیت نمی‌شود.
2. **هیچ رفتار سالمی بازنویسی نمی‌شود** (قانون ۳ پروژه)؛ تغییرات حداقلی و مستند.
3. **ادعای اثبات‌نشده ممنوع** (قانون ۹): هر مورد یا تست دارد یا شاهد اجرایی.
4. **تصمیم‌های عضوی از تیم به‌جای شما گرفته نمی‌شود**: هر جا تصمیم تجاری لازم شد (مثل درگاه پرداخت)، فقط آماده‌سازی می‌کنم و علامت می‌زنم.

---

## نتیجه اجرا (شاهدها) — ۲۰۲۶-۰۹-۲۰

| کار | شاهد اجرا |
|:-:|---|
| ۱ | `data/manto-moda.json`؛ تست دستی: ثبت سفارش `MM-ORD-2026-9269` → خاموشی کامل → ری‌استارت → «✅ بارگذاری از اسنپ‌شات، تاریخچه حفظ شد». در حالت test ماندگاری خاموش است تا تست‌ها داده واقعی را لمس نکنند. |
| ۲ | `409 INSUFFICIENT_STOCK` با `{requested: 9999, available: 15}`؛ خرید معتبر ۱۵ → ۱۳؛ تلاش دوم برای باقی‌مانده هم رد شد. |
| ۳ | `PENDING → DELIVERED` رد شد با `{"error":"INVALID_STATUS_TRANSITION","allowedTransitions":["CONFIRMED","CANCELLED"]}`؛ زنجیره مجاز CONFIRMED → PROCESSING → SHIPPED هر گام با ثبت در `statusHistory` (۴ رکورد). |
| ۴ | چهار پیامک فارسی در لاگ سرور: «سفارش … ثبت شد / تأیید شد / در حال آماده‌سازی است / ارسال شد»؛ سفارش با شماره ۰۹۰۰… خطای ارسال را «ثبت» کرد ولی ثبت سفارش و اقدام ادمین موفق ماند. |
| ۵ | خروجی SIGTERM: «Pending data snapshot flushed to disk → HTTP server closed cleanly»؛ سرور با `unhandledRejection` زنده می‌ماند و روی `uncaughtException` داده را flush و با کد غیرصفر خارج می‌شود. |
| ۶ | `npm test` = ۷ مجموعه سبز؛ مجموعه هفتم (`tests/operations.test.js`) مسیرهای HTTP واقعی را با supertest اجرا می‌کند و همین تست یک باگ واقعی (`path is not defined`) را بعد از جدا کردن app/index گرفت. |
| ۷ | `meta: {page:1, limit:3, total:5, hasMore:True}`؛ `limit=9999` به ۶۰ محدود شد. |
| ۸ | آپلود JPEG ۱۰۰۰×۱۳۰۰ → ذخیره WebP + بندانگشتی، سرو شدن با `content-type: image/webp`؛ PHP جعلی با MIME عکس → `415 INVALID_IMAGE_CONTENT`؛ کاربر عادی → `403`؛ traversal → `404`. |
| ۹ | `robots.txt` + سایت‌مپ با ۱۲ آدرس؛ صفحه `/product/manto-kati-eliza` یک `<title>`، یک `og:title`، یک canonical و JSON-LD شامل Product + Offer + قیمت IRR. |
| ۱۰ | `npm run a11y` = ۱۷ بررسی سبز (این ابزار دو نقص واقعی را پیدا کرد و اصلاح شد: رادیوهای روش پرداخت و ۵ فیلد بدون برچسب). |
| ۱۱ | `bash scripts/backup.sh` → ساخت، بررسی سلامت آرشیو، چرخش؛ `--restore` روی مسیر تازه و صحت‌سنجی محتوا. |
| ۱۲ | ADR-010..۰۱۶ در `DECISIONS.md`، بخش ۱۲ در `ARCHITECTURE.md`، CHANGELOG/TASKS/README/project-state.json به‌روز، CI با گیت دسترس‌پذیری و smoke test پشتیبان، کامیت + بسته تازه. |

**دروازه کیفیت نهایی:** `npm test` (۷ مجموعه) + `npm run a11y` (۱۷ بررسی) — هر دو سبز.
