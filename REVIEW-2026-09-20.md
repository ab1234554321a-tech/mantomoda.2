# گزارش بازبینی کامل پروژه — مانتو مُدا
**تاریخ:** ۲۰۲۶-۰۹-۲۰ | **دامنه بازبینی:** کل پروژه (کد، امنیت، تست، مستندات، تجربه مشتری، آمادگی تجاری)
**روش:** اجرای واقعی همه اسکیل‌های اجرایی + بازبینی دستی کد و سرور زنده + مسیر طلایی end-to-end

---

## ۱. خلاصه مدیریتی

بازبینی کامل انجام شد و **سه مشکل جدی** پیدا شد که هیچ‌کدام با تست‌های قبلی گرفته نمی‌شدند. مهم‌ترینشان این بود:

> **هر غریبه‌ای روی اینترنت می‌توانست با یک درخواست ساده، توکن مدیر فروشگاه بگیرد و سفارش‌ها، نام و تلفن و آدرس همه مشتریان را بخواند، قیمت‌ها را عوض کند و کالاها را آرشیو کند.**

این نقص از ابزارهای «شبیه‌ساز نقش» می‌آمد که برای نمایش پروژه ساخته شده بود و هنگام انتشار واقعی بسته نشده بود. الان بسته است و با ۹ بررسی خودکار قفل شده.

| # | مشکل | اگر پیدا نمی‌شد | وضعیت |
|---|---|---|---|
| ۱ | شبیه‌ساز نقش، توکن مدیر بدون رمز می‌داد (ADR-024) | تسخیر کامل پنل توسط هر بازدیدکننده | ✅ اصلاح + تست سطح ۱۰ |
| ۲ | رمز حساب‌های نمونه در مخزن عمومی گیت‌هاب بود | ورود به پنل با رمز شناخته‌شده | ✅ اصلاح + تست سطح ۱۰ |
| ۳ | فروشگاه واقعی با کالا و سفارش و مشتری جعلی بالا می‌آمد | داشبورد درآمد جعلی، داده ساختگی روی سایت زنده | ✅ اصلاح (فروشگاه خالی بالا می‌آید) |
| ۴ | هیچ صفحه قوانین/مرجوعی/حریم‌خصوصی/تماس وجود نداشت | **تأیید درگاه پرداخت ناممکن** (شرط شاپرک/زرین‌پال) | ✅ اصلاح (ADR-025) |
| ۵ | ۴۹ endpoint بدون قرارداد ماشین‌خوان | موبایل‌اپ/توسعه آینده روی حدس می‌ساخت | ✅ `openapi.yaml` + بازرس خودکار |
| ۶ | نسخه در `/api/health` یک انتشار عقب بود | پایش سرور نسخه اشتباه گزارش می‌کرد | ✅ از `package.json` خوانده می‌شود |
| ۷ | CI کنترل concurrency نداشت | اجراهای موازی، نتیجه اشتباه روی PR | ✅ اصلاح |

**نتیجه بازریسی اسکیل‌ها: ۱۱ بررسی سالم، ۰ هشدار، ۰ ایراد.** (۴۸ یافته کاذب اسکنر، مستند و کنار گذاشته شد — بخش ۵)

---

## ۲. شماره‌های پروژه

| شاخص | قبل از این بازبینی | بعد |
|---|---|---|
| مجموعه تست | ۹ | **۱۲** (سطح ۱۰ امنیت پروداکشن، ۱۱ مجوزدهی، ۱۲ صفحات) |
| endpoint مستندشده | ۰ | **۴۴ مسیر / ۵۰ عملیات** در `openapi.yaml` |
| ADR | ۲۳ | **۲۵** |
| بررسی اسکیل‌ها | ۱۰ سالم / ۱ هشدار | **۱۱ سالم / ۰ هشدار / ۰ ایراد** |
| کامیت‌ها | ۲۱ | **۲۹** (۸ کامیت در این مرحله) |
| زمان تست کامل | ۱۱ ثانیه | ۱۱.۶ ثانیه (۱۲ مجموعه) |

---

## ۳. آنچه ساخته/اصلاح شد

### الف) امنیت (بحرانی)
- **ماژول واحد حالت اجرا** (`src/server/utils/runtime-mode.js`): تنها جایی که تصمیم می‌گیرد اجرای تولیدی چه اجازه‌ای دارد.
- شبیه‌ساز نقش در پروداکشن **۴۰۴** می‌دهد (نه ۴۰۳ — تا وجودش را تبلیغ نکند) و منوی «مدیر سیستم» از دید بازدیدکننده **پنهان** است.
- حساب‌های نمونه: در پروداکشن ساخته نمی‌شوند، از نسخه‌های قبلی داده حذف می‌شوند، و در **دو لایه** (مسیر ورود + middleware احراز هویت) رد می‌شوند تا توکن قدیمی هم بی‌اثر باشد. پیام رد، دقیقاً مثل رمز اشتباه است (بدون افشای وجود حساب).
- **حساب مدیر واقعی** از `ADMIN_EMAIL`/`ADMIN_PASSWORD`: سرور بدون آن بالا نمی‌آید؛ نصب‌کننده سرور رمز قوی می‌سازد و **یک‌بار** چاپ می‌کند؛ preflight نبودن/ضعیف‌بودنش را به‌عنوان مانع انتشار گزارش می‌دهد.
- فروشگاه واقعی **خالی** شروع می‌شود.

### ب) آمادگی تجاری (مانع درگاه پرداخت)
- پنج صفحه سرور-رندر: `/page/terms`، `/page/returns`، `/page/privacy`، `/page/sizing`، `/page/contact` — با متادیتای مستقل، در `sitemap.xml`، و لینک‌شده در فوتر (قبلاً «متن خشک» بود).
- صفحه تماس، تلفن و نشانی را از **تنظیمات فروشگاه** می‌خواند — همان مقادیری که روی فاکتور چاپ می‌شود (یک منبع حقیقت).
- سیاست‌های تجاری (مهلت مرجوعی، زمان بازگشت وجه، ساعات پاسخ‌گویی) با پیش‌فرض‌های منطقی نوشته و با نشان `TODO-OWNER` علامت خورده‌اند: نشان هرگز به مشتری نشان داده نمی‌شود و `preflight` تا تأیید نشدنشان هشدار می‌دهد. **پیش‌فرض‌ها جای مشورت حقوقی را نمی‌گیرند؛ قبل از انتشار تأییدشان کن.**

### ج) قرارداد و ابزار
- `openapi.yaml`: ۴۴ مسیر، ۵۰ عملیات، ۴۸ اسکیما (بدنه درخواست، پاسخ‌ها، نقش‌ها، محدودیت نرخ، استثنای حالت دمو).
- `npm run api:check`: سطح واقعی API را از کد استخراج می‌کند و اگر کد و مستند در هر جهت ناهمخوان باشند، build را قرمز می‌کند (تست جهش شد).
- `npm run verify:golden`: کل سفر مشتری و مدیر روی HTTP واقعی، با یک خط گزارش در هر گام.

### د) تست‌های جدید
| سطح | چه چیزی را قفل می‌کند |
|---|---|
| ۱۰ (Production guards) | ۹ بررسی: ۴۰۴ بودن شبیه‌ساز، رد رمزهای منتشرشده، ورود مدیر واقعی، فروشگاه خالی، `/api/config`، بی‌اثر بودن توکن حساب نمونه، امتناع از بوت بدون حساب مدیر، رد رمز ضعیف، و سالم‌ماندن حالت توسعه |
| ۱۱ (Authorization sweep) | همه مسیرهای `/api/admin` را **از خود کد** استخراج می‌کند و می‌سنجد غریبه/مشتری/عمده‌فروش رد شوند + کنترل مثبت مدیر + بازماندن سطح عمومی |
| ۱۲ (Editorial pages) | محتوا، متادیتا، RTL، لینک فوتر، سایتمپ، اتصال به تنظیمات، escape شدن، ۴۰۴ |

---

## ۴. یافته‌های بازبینی که **مشکل نبودند** (تا اشتباه دنبال نشود)

| موضوع | نتیجه بررسی |
|---|---|
| ۲۰ مسیر `/api/admin` بدون `requireRole` در خط تعریف | ❌ کاذب — محافظ سطح router است: `router.use(requireRole('ADMIN'))`. آزمون زنده با توکن مشتری روی ۲۲ مسیر: همه رد شدند (۴۰۱/۴۰۳)، توکن مدیر: ۲۰۰ |
| ۳۹ هشدار `innerHTML` | بررسی دستی: فقط یک زنجیره واقعاً قابل بهره‌برداری بود (ADR-022، اصلاح شد). بقیه رندر HTML ثابت‌اند یا با `textContent` امن شده‌اند |
| ۷ مورد «CRITICAL: تزریق SQL» | ❌ کاذب — پروژه هیچ SQL ندارد (ذخیره‌سازی JSON snapshot است، ADR-010) |
| «Short password constant» | ❌ کاذب — اسم فیلدهای حساس در لیست پاک‌سازی لاگ است، نه رمز |
| «Non-production check» در `otp.service.js` | ❌ کاذب — همان خط، محافظ محیطی موردنظر است |
| ۱۰ شناسه `set-*` که در HTML نبود | ❌ کاذب — فرم تنظیمات با helper ساخته می‌شود (`id="${id}"`)، تعریفش در JS است |
| `docker-compose.yml` دارای 127.0.0.1 | ✅ عمدی — Node پشت Nginx، پورت فقط روی لوکال‌بایند است (ADR-021) |
| ترتیبِ `payable` در پاسخ سبد / کد خطای گذار سفارش | 🔧 اشتباه **در مستند من** بود، نه در کد: پاسخ‌ها `payableAmount`/`shippingFee` هستند و گذار غیرقانونی ۴۰۹ می‌دهد. هر دو اصلاح شد |

---

## ۵. وضعیت اسکیل‌ها (پاسخ عملی به «اسکیل به چه دردی می‌خورد»)

سه اسکیل در این مرحله کار واقعی کردند:

| اسکیل | یافته‌اش | نتیجه |
|---|---|---|
| `security-auditor` | الگوهای OWASP روی `src/` | زمینه‌ساز کشف زنجیره XSS پنل مدیر (ADR-022) و شماره سفارش تکراری؛ در این دور، صفر یافته تازه در کد تولید |
| `api-architect` | «هیچ OpenAPI یافت نشد» + هشدار localhost در compose | ساخت `openapi.yaml` + بازرس خودکار قرارداد |
| `devops-automator` | CI فاقد کنترل concurrency | اصلاح شد |
| `technical-writer` | پوشش مستندات و ADR | پاس (۲۵ ADR) |

**قاعده‌ای که در `SECURITY-TRIAGE.md` ثبت شده و اجرا می‌شود:** اسکنر *کاندید* تولید می‌کند، نه *حکم*. هر یافته دستی راستی‌آزمایی می‌شود؛ آنچه واقعی است اصلاح و به تست ارتقا داده می‌شود، آنچه کاذب است با دلیل و «چه چیزی این کاذب را در آینده واقعی می‌کند» مستند می‌شود.

بازرسی کامل: `npm run skills-audit` → گزارش در `reports/`.

---

## ۶. کاری که هنوز مانده (شفاف)

| موضوع | مالک | چرا |
|---|---|---|
| تأیید ۶ سیاست تجاری علامت‌خورده در `src/server/content/pages.js` | مالک فروشگاه | پیش‌فرض‌ها منطقی‌اند اما تصمیم تجاری‌اند، نه فنی |
| کلید زرین‌پال + کاوه‌نگار | مالک فروشگاه | احراز هویت و قرارداد؛ کد آماده است (ADR-008/009) |
| سرور + دامنه | مالک فروشگاه | یک دستور نصب آماده است (HOSTING-GUIDE.md) |
| فایل `.github/workflows/ci.yml` روی گیت‌هاب | — | نیاز به دسترسی `workflow` توکن دارد؛ کامیتش محلی آماده است و CI بدون آن هم پروژه را متوقف نمی‌کند |
| تست بار/مقیاس (چند صد کاربر همزمان) | فنی | پیش از کمپین تبلیغاتی؛ ذخیره‌سازی در حافظه است (سقف شناخته‌شده، TD-006) |

---

## ۷. تأییدهای عملی (نه ادعا)

```
✅ ۱۲ مجموعه تست سبز (۱۱.۶ ثانیه)
✅ ۱۷/۱۷ بررسی دسترس‌پذیری
✅ npm audit: ۰ آسیب‌پذیری
✅ قرارداد API با کد هم‌خوان (۵۰ عملیات)
✅ بازرسی اسکیل‌ها: ۱۱ سالم / ۰ هشدار / ۰ ایراد
✅ مسیر طلایی end-to-end: کامل و بی‌ایراد روی HTTP واقعی
✅ نسخه روی گیت‌هاب: کلون، نصب production و بوت موفق (HTTP 200)
```
