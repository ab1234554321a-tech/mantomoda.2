# IMPROVEMENT_PLAN.md — برنامه بهبود و مسیر پیشرفت

> **سند تصمیم‌گیری.** بر پایه بررسی مستقیم کد (نه حدس) در تاریخ ۲۰۲۶-۰۹-۲۰.
> هر یافته «شاهد» (فایل + خط) دارد. هر پیشنهاد «اثر / هزینه / مهارت‌های لازم» دارد.
> وضعیت پیشرفت پروژه: **۹۲٪** طبق `PROJECT_STATE.md` — اما درصد تکمیل *کد* با «آماده فروش بودن» یکی نیست.
> این سند همان فاصله را مشخص می‌کند.

---

## بخش ۱ — یافته‌های بررسی (با شاهد)

### 🔴 P0 — مانع فروش واقعی

#### ۱. موجودی انبار نه بررسی می‌شود، نه کم می‌شود `(Overselling)`
- **شاهد**: در `src/server/routes/order.routes.js` هیچ ارجاعی به `stock` وجود ندارد. در `src/server/db/store.js` هیچ متدی موجودی را تغییر نمی‌دهد (فقط در ساخت محصول مقدار اولیه می‌گیرد).
- **پیامد**: مشتری می‌تواند ۱۰۰ عدد از محصولی با موجودی ۳ عدد سفارش دهد؛ سفارش ثبت می‌شود، پول می‌آید و کالا وجود ندارد. برای یک مزون این یعنی تماس عذرخواهی، مرجوعی و ریزش مشتری.
- **اثر**: بحرانی | **هزینه**: کم (۱ روز) | **مهارت‌ها**: `postgresql-optimization` (قفل/تراکنش)، `vitest-testing-patterns`

#### ۲. همه داده‌ها با هر ری‌استارت پاک می‌شوند
- **شاهد**: `DataStore` در `store.js` فقط از `SEED_*` ساخته می‌شود. `BACKLOG.md` → `BL-005` خودش این را «Evidence» کرده است. کدهای OTP و نشست‌های پرداخت هم الان در همین حافظه‌اند.
- **پیامد**: با هر deploy، ری‌استارت سرور، یا کرش: همه کاربران، سفارش‌ها، تأییدهای عمده و کدهای در جریان **از بین می‌روند**. عملاً فروشگاه فقط تا اولین ری‌استارت کار می‌کند.
- **اثر**: بحرانی | **هزینه**: متوسط | **مهارت‌ها**: `database-design-patterns`، `postgresql-optimization`، `refactoring-surgeon`، `docker-containerization`

#### ۳. مشتری از تغییر وضعیت سفارش باخبر نمی‌شود
- **شاهد**: `admin.routes.js:139` وضعیت را تغییر می‌دهد؛ هیچ فراخوانی‌ای به سرویس پیامک (که همین دیروز ساختیم) وجود ندارد.
- **پیامد**: بزرگ‌ترین سؤال مشتری «سفارشم کجاست؟» از طریق تماس تلفنی می‌آید. برای مزون = هزینه پشتیبانی و نارضایتی.
- **اثر**: بالا | **هزینه**: کم (نیم روز) — آداپتر کاوه‌نگار آماده است، فقط وصل شود | **مهارت‌ها**: `background-job-orchestrator`

#### ۴. ماشین وضعیت سفارش واقعی نیست
- **شاهد**: `admin.routes.js:141` فقط بررسی می‌کند که وضعیت *در فهرست مجاز باشد*؛ نه اینکه *گذار مجاز باشد*. یعنی `DELIVERED → PENDING` یا `CANCELLED → SHIPPED` ممکن است.
- **پیامد**: خطای انسانی در پنل، وضعیت مالی/انبار را خراب می‌کند. بدون لاگ تغییر وضعیت هم ردیابی ممکن نیست.
- **اثر**: متوسط | **هزینه**: کم | **مهارت‌ها**: `checklist-discipline`، `error-handling-patterns`

### 🟠 P1 — مانع رشد و دیده‌شدن

#### ۵. SEO عملاً صفر است (بدون کانال ارگانیک)
- **شاهد**: `index.html` فقط **۱** متا تگ دارد؛ `robots.txt` و `sitemap.xml` وجود ندارد؛ هیچ `Open Graph`، `Twitter Card` یا `schema.org/Product` نیست؛ کل محتوا سمت کلاینت رندر می‌شود.
- **پیامد**: گوگل محصولات را ایندکس نمی‌کند. تنها راه فروش = تبلیغ پول‌محور. برای مزون، عکس محصول در نتایج گوگل («مانتو کتی الیزا») معمولاً ارزان‌ترین کانال فروش است.
- **اثر**: بالا | **هزینه**: متوسط-بالا | **مهارت‌ها**: `ultimate-seo-geo-skill`، `seo-visibility-expert`، `nextjs-app-router-expert`

#### ۶. عکس واقعی محصول نمی‌شود گذاشت
- **شاهد**: هیچ `multer`/`multipart`/آپلود در پروژه نیست. همه تصاویر URL خارجی (Unsplash) هستند — `store.js:256`.
- **پیامد**: پنل ادمین می‌تواند محصول بسازد، ولی فقط با لینک عکس اینترنتی. برای مزون **عکس = کالا**؛ بدون آپلود، هیچ محصول واقعی ثبت نمی‌شود.
- **اثر**: بالا | **هزینه**: متوسط | **مهارت‌ها**: `document-generation-pdf` نیست؛ `form-validation-architect`، `pwa-expert` (بهینه‌سازی تصویر)

#### ۷. دسترس‌پذیری ضعیف
- **شاهد**: در `app.js` فقط **۱** `alt` وجود دارد (کارت اصلی)؛ گالری محصول، سبد خرید و لیست سفارش‌ها بدون `alt`. صفر `aria-*` در کل پروژه. فرم‌ها فقط `placeholder` دارند، بدون `<label>`.
- **پیامد**: کاربران صفحه‌خوان نمی‌توانند خرید کنند؛ هم‌چنین Google از دسترس‌پذیری به‌عنوان سیگنال کیفیت استفاده می‌کند. الزام قانونی برای فروشگاه‌های بزرگ‌تر هم وجود دارد.
- **اثر**: متوسط | **هزینه**: کم-متوسط | **مهارت‌ها**: `design-accessibility-auditor`، `color-contrast-auditor`، `typography-expert`

#### ۸. بدون صفحه‌بندی و مقیاس‌پذیری کاتالوگ
- **شاهد**: `product.routes.js` هیچ `page`/`limit`/`offset` ندارد؛ همه محصولات در یک پاسخ برمی‌گردند.
- **پیامد**: با ۵۰۰ محصول، هر بازدید کاتالوگ = انتقال چند مگابایت + کندی روی موبایل و اینترنت ضعیف.
- **اثر**: متوسط (با رشد) | **هزینه**: کم | **مهارت‌ها**: `react-performance-optimizer`، `caching-strategies`

### 🟡 P2 — پایداری و بلوغ

| # | یافته | شاهد | اثر |
|---|---|---|---|
| ۹ | بدون مدیریت خطای سطح process (`unhandledRejection`/`uncaughtException`) | فقط `SIGTERM`/`SIGINT` در `index.js` | سرور می‌تواند بی‌صدا بمیرد |
| ۱۰ | بدون پشتیبان‌گیری | `ROADMAP.md` گفته، کدی نیست | از دست رفتن داده = فاجعه |
| ۱۱ | بدون CI | **رفع شد** — `.github/workflows/ci.yml` اضافه شد | جلوی رگرسیون گرفته می‌شود |
| ۱۲ | ۳ آسیب‌پذیری متوسط وابستگی (`qs`) | `npm audit` | **رفع شد** — الان ۰ آسیب‌پذیری |
| ۱۳ | ادعای نادرست React/TS/Vite/Vitest در مستندات | `README.md:55-60`، `ARCHITECTURE.md:12` | **رفع شد** — هم‌راستا با واقعیت Vanilla JS |

---

## بخش ۲ — پیشنهاد من: سه گام

### 🥇 گام ۱ — «فروش قابل اعتماد» (بدون نیاز به هیچ تصمیم تجاری)
هدف: تبدیل پروژه از «دموی کارآمد» به «فروشگاهی که امشب می‌توان روش فروش کرد».

| کار | چرا اول این؟ |
|---|---|
| یکپارچگی انبار (بررسی + کاهش اتمیک + بازگردانی در لغو) | جلوگیری از فروش کالای ناموجود — تنها موردی که مستقیماً اعتبار برند را می‌سوزاند |
| ماندگاری داده (Postgres/Prisma، و تا آماده شدن سرور: لایه فایل) | بدون آن هر ری‌استارت = پاک شدن همه سفارش‌ها |
| اعلان پیامکی تغییر وضعیت سفارش | آداپتر کاوه‌نگار آماده است؛ نیم روز کار، بیشترین کاهش تماس پشتیبانی |
| ماشین وضعیت + لاگ تغییرات + مدیریت خطای process | جلوگیری از خرابی خاموش |
| CI (انجام شد) | تضمین اینکه کارهای بالا چیزی را نشکنند |

**حاصل**: مشتری می‌تواند با اطمینان خرید کند و سفارشش را پیگیری کند؛ شما می‌توانید با اطمینان deploy کنید.

### 🥈 گام ۲ — «دیده شدن و فروش»
هدف: مشتری بدون تبلیغ هم از گوگل بیاید.

| کار | چرا؟ |
|---|---|
| آپلود عکس محصول (+ WebP و بندانگشتی) | بدون آن هیچ محصول واقعی ثبت‌شدنی نیست |
| SEO: `sitemap.xml`، `robots.txt`، OG، `schema.org/Product` | ایندکس شدن در گوگل و نمایش قیمت/موجودی در نتایج |
| صفحه اختصاصی محصول به‌جای مودال + URL قابل اشتراک | لینک محصول در اینستاگرام/واتساپ = مهم‌ترین کانال فروش پوشاک ایرانی |
| دسترس‌پذیری (alt/aria/label/کنتراست) | هم مشتری بیشتر، هم سیگنال مثبت گوگل |
| صفحه‌بندی + جست‌وجوی بهتر | سرعت روی موبایل و اینترنت ضعیف |

**نکته فنی مهم**: برای SEO واقعی، رندر سمت سرور لازم است. دو مسیر دارد:
- **مسیر سبک (پیشنهادی برای الان)**: صفحه محصول به‌صورت HTML تولیدشده در سرور (prerender) + همان SPA. هزینه کم، بدون بازنویسی.
- **مسیر سنگین** (`TD-005`): مهاجرت به Next.js. اثر بیشتر، اما بازنویسی کل فرانت‌اند.

### 🥉 گام ۳ — «مقیاس و سود»
| کار | چرا؟ |
|---|---|
| Redis برای کش کاتالوگ و OTP | سرعت زیر ۵ms و ماندگاری کدها (TD-004) |
| داشبورد تحلیلی: فروش، نرخ تبدیل، سبد رهاشده | تصمیم بر پایه داده، نه حس |
| B2B حرفه‌ای: حداقل سفارش عمده، لیست قیمت هر مزون، اعتبار | مشتری عمده، سودآورترین بخش این مدل کسب‌وکار است |
| سبد خرید رهاشده + پیامک یادآوری | معمولاً بزرگ‌ترین منبع فروش از دست رفته |

---

## بخش ۳ — پیشنهاد صریح من

**از گام ۱ شروع کنیم، به این ترتیب دقیق:**

1. **یکپارچگی انبار** — چون تنها باگ واقعی موجود است که مشتری لمسش می‌کند.
2. **اعلان پیامکی سفارش** — کم‌هزینه‌ترین کار با بیشترین اثر بر رضایت مشتری (آداپتر آماده است).
3. **ماندگاری داده** — تصمیم فنی: تا وقتی سرور و Postgres شما آماده نشده، یک لایه ذخیره‌سازی فایل (`data/*.json` + نوشتن اتمیک) پیاده می‌کنم که از طریق همان `store.js` کار کند. بعداً Postgres فقط جای این لایه را می‌گیرد (بدون تغییر در مسیرها و تست‌ها).

**چرا نه SEO اول؟** چون SEO وقتی معنا دارد که محصول قابل ثبت باشد (عکس) و موجودی معتبر باشد. الان هر ترافیکی که بیاید، به سفارش کالای ناموجود می‌رسد.

**چرا نه پرداخت اول؟** خودتان گفتید بعداً. ضمناً پرداخت تا وقتی انبار و داده معتبر نباشند، فقط پول را وارد یک سیستم ناپایدار می‌کند.

---

## بخش ۴ — چیزهایی که لازم نیست انجام دهیم (ضد فرسودگی)

- **مهاجرت زودهنگام به React/Next.js**: `app.js` با ۱۱۳۰ خط، خوانا و سریع است. بازنویسی الان = هفته‌ها کار بدون فروش بیشتر. فقط اگر SEO مسیر سبک جواب نداد.
- **TypeScript در کل بک‌اند**: پروژه بدون آن هم تست دارد و کار می‌کند. مهاجرت تدریجی، فقط روی فایل‌های جدید.
- **Kubernetes / معماری چندسرویسی**: برای این مقیاس، یک سرور + Postgres + Redis کافی است.
- **افزودن اسکیل بیشتر**: ۶۹ اسکیل کافی است. گلوگاه این پروژه **ابزار نیست، کارهای P0 بخش ۱ است**.

---

## بخش ۵ — نقشه مهارت‌ها (کدام اسکیل برای کدام کار)

| کار | اسکیل‌های منتخب از ۶۹ مورد نصب‌شده |
|---|---|
| یکپارچگی انبار | `postgresql-optimization`، `vitest-testing-patterns`، `checklist-discipline` |
| ماندگاری داده | `database-design-patterns`، `refactoring-surgeon`، `docker-containerization` |
| اعلان پیامکی | `background-job-orchestrator`، `error-handling-patterns` |
| ماشین وضعیت سفارش | `code-architecture`، `checklist-discipline` |
| SEO | `ultimate-seo-geo-skill`، `seo-visibility-expert`، `nextjs-app-router-expert` |
| آپلود و بهینه‌سازی عکس | `pwa-expert`، `form-validation-architect`، `design-critic` |
| دسترس‌پذیری | `design-accessibility-auditor`، `color-contrast-auditor`، `typography-expert` |
| پایداری (SRE) | `logging-observability`، `launch-readiness-auditor`، `devops-automator` |
| رشد و B2B | `competitive-cartographer`، `product-appeal-analyzer`، `data-viz-2025` |

---

## بخش ۶ — معیار «آماده فروش بودن» (Definition of Done پروژه)

پروژه وقتی واقعاً آماده فروش است که این ده مورد ✅ باشند:

1. موجودی هر تنوع کالا درست کم و زیاد شود و فروش بیش از موجودی ممکن نباشد.
2. ری‌استارت سرور هیچ سفارشی را پاک نکند.
3. مشتری با هر تغییر وضعیت، پیامک دریافت کند.
4. گذارهای وضعیت سفارش فقط در مسیرهای مجاز ممکن باشد و ثبت شوند.
5. هر محصول عکس واقعی خودش را داشته باشد (آپلودشده، بهینه، بدون وابستگی به سرویس خارجی).
6. هر محصول یک صفحه اختصاصی با URL قابل اشتراک و متا تگ درست داشته باشد.
7. `sitemap.xml` و `robots.txt` در دسترس باشند و محصولات در Google Search Console ثبت شوند.
8. صفحه با صفحه‌خوان و بدون ماوس قابل استفاده باشد؛ کنتراست حداقل AA.
9. هر تغییر روی `main` از CI سبز بگذرد (اکنون برقرار است).
10. پشتیبان‌گیری روزانه خودکار و **تست‌شده** برای بازیابی وجود داشته باشد.

**وضعیت فعلی: ۳ از ۱۰** (موردهای ۹ و بخشی از ۶ و ۴).
